using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScriptOculta : MonoBehaviour
{
    public GameObject plataforma; 
    void Start()
    {
        plataforma.SetActive(false); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
